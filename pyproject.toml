[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "web-automator"
version = "0.2.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["web_automator"]
